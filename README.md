# `Any2Any-Transformer`
`Any2Any-Transformer` is a transformer project aimed at exploring the possibility of using different encoder and decoders while not loosing context.
This is of interest as one might be able to cut down on hard to aquire (and therefore expensive) datasets for such a model.
